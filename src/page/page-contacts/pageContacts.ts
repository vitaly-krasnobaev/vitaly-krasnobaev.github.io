import '@/page/page-contacts/page-contacts.scss';
import Page from '@/page/Page';

const content = `
  <h1>Contacts</h1>
  <a href="#navigation">Back</a>
`;

export default new Page({ slug: 'contacts', content });
